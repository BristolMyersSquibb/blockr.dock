% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-dock.R
\name{is_dock_locked}
\alias{is_dock_locked}
\alias{req_unlocked}
\title{Dock locked-mode trust boundary}
\usage{
is_dock_locked()

req_unlocked()
}
\value{
\code{is_dock_locked()} returns a single \code{TRUE} / \code{FALSE}.
\code{req_unlocked()} returns invisibly when the dock is unlocked; when
locked it raises a silent \code{\link[shiny:req]{shiny::req()}} failure so the enclosing
\code{observeEvent} skips the handler.
}
\description{
Helpers for respecting the \code{blockr.dock_is_locked} option in custom
extensions and downstream packages. \code{is_dock_locked()} is the
predicate used by UI builders to skip rendering write controls;
\code{req_unlocked()} is the matching server-side gate that short-circuits
the trigger of any state-mutating \code{\link[shiny:observeEvent]{shiny::observeEvent()}}, so a
forged \code{Shiny.setInputValue()} cannot bypass the UI hide.
}
\details{
UI hides (\code{display: none}, missing buttons, \code{\link[=view_can_crud]{view_can_crud()}}) are
the obvious defence; they leave the underlying Shiny inputs live so a
client can still flip them via \code{Shiny.setInputValue()}. Pair every UI
gate with \code{req_unlocked()} in the \emph{trigger} of any state-mutating
observer:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{observeEvent(
  \{
    req_unlocked()
    input$confirm_view_add
  \},
  \{ ... mutation ... \}
)
}\if{html}{\out{</div>}}

Placing the check in the \code{eventExpr} (rather than the handler body)
makes the trigger itself short-circuit when locked: no handler
invocation, no observer span in OpenTelemetry traces, no log noise.
\code{is_dock_locked()} reads a non-reactive base R option (set via
\code{options(blockr.dock_is_locked = TRUE)}), so this adds no spurious
reactive dependencies.
\subsection{Extension authors}{

\code{blockr.dock} does not auto-hide extensions in locked mode. The
right behaviour depends on the extension and is left to its author.
Pick whichever of these fits the use case:
\itemize{
\item \strong{Hide the panel entirely} when locked. Return \code{NULL} (or an
empty \code{tagList()}) from the extension's \code{ui} function when
\code{is_dock_locked()} is \code{TRUE}. Best when the extension only makes
sense as an authoring tool (e.g. a board editor).
\item \strong{Render read-only} when locked. Keep the panel visible but drop
write controls, and gate any remaining mutating observers with
\code{req_unlocked()}. Useful when the extension offers inspection
value beyond editing (e.g. a DAG visualiser).
\item \strong{Gate mutations only}. Keep the UI intact and wrap every
state-mutating observer's \code{eventExpr} with \code{req_unlocked()}. The
simplest path when the UI is already read-only by nature.
}
}
}
\examples{
# In an extension's mutating observer:
\dontrun{
observeEvent(
  {
    req_unlocked()
    input$my_confirm
  },
  {
    update(list(blocks = list(add = new_blk)))
  }
)
}

}
