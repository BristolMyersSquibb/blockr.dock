% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sidebar-api.R
\name{sidebar}
\alias{sidebar}
\alias{sidebar-api}
\alias{show_sidebar}
\alias{hide_sidebar}
\title{Sidebar API}
\usage{
show_sidebar(id, ..., session = shiny::getDefaultReactiveDomain())

hide_sidebar(session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{id}{The sidebar type ID (e.g., "add_block"). Used for S3 dispatch.}

\item{...}{Named context values to store (passed to \code{set_sidebar_context})}

\item{session}{Shiny session (default: current reactive domain)}
}
\description{
A slide-out sidebar system for blockr.dock. The sidebar dynamically renders
content based on S3 dispatch, allowing extensions to add custom sidebar types
by defining \verb{sidebar_content_ui.*} methods.
}
\section{Functions}{

\itemize{
\item \code{\link[=show_sidebar]{show_sidebar()}} - Show sidebar with specific content section
\item \code{\link[=hide_sidebar]{hide_sidebar()}} - Hide the sidebar
\item \code{\link[=set_sidebar_context]{set_sidebar_context()}} - Set context data for sidebar renderers
\item \code{\link[=get_sidebar_context]{get_sidebar_context()}} - Retrieve context data
}
}

\section{Usage}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Show sidebar with the "add_block" content type
show_sidebar("add_block")

# With context data
set_sidebar_context(source_block = "blk_1")
show_sidebar("add_link")

# Hide
hide_sidebar()
}\if{html}{\out{</div>}}
}

\section{For Extensions}{

Extensions can add custom sidebar types by defining S3 methods:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{sidebar_content_ui.my_sidebar <- function(type, ns, board, ...) \{
  list(
    title = "My Sidebar",
    subtitle = "Optional subtitle",
    show_search = FALSE,
    content = sidebar_panel(
      body = tagList(...),
      footer = actionButton(ns("confirm"), "Confirm")  # optional
    )
  )
\}
}\if{html}{\out{</div>}}

Then trigger with \code{show_sidebar("my_sidebar")}.
}

