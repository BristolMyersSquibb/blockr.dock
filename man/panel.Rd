% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-ui.R
\name{show_panel}
\alias{show_panel}
\title{Show a panel}
\usage{
show_panel(id, board, dock, type = c("block", "extension"))
}
\arguments{
\item{id}{Object ID}

\item{board}{Board object}

\item{dock}{Object available as \code{dock} in extensions}

\item{type}{Either "block" or "extension", the kind of panel to show}
}
\value{
\code{NULL}, invisibly.
}
\description{
\code{show_panel()} brings a block or extension panel into view in the live
\code{dock}. It is \strong{deprecated}: opening a panel is now expressed through the
\code{views} update grammar, which an extension composes directly and passes to
\code{update()} -- switching to a view that holds the panel and selecting its tab,
\code{update(list(views = list(active = v, mod = list(v = list(select = p)))))},
with a \code{mod} \code{add} entry first for an unplaced panel. That path needs no live
\code{dock} handle, which is no longer on the extension server surface.
}
