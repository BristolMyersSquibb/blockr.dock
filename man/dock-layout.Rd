% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dock-layout.R
\name{dock-layout}
\alias{dock-layout}
\alias{new_dock_layout}
\alias{is_dock_layout}
\alias{validate_dock_layout}
\alias{as_dock_layout}
\title{Dock layout: dockView's native representation}
\usage{
new_dock_layout(x = list())

is_dock_layout(x)

validate_dock_layout(x)

as_dock_layout(x, ...)
}
\arguments{
\item{x}{Object to wrap, validate, test, or cast.}

\item{...}{Passed on to methods.}

\item{blocks, extensions}{Board components used to resolve panel content
when casting a \code{dock_grid} to a \code{dock_layout}.}
}
\value{
\code{new_dock_layout()}, \code{validate_dock_layout()} and \code{as_dock_layout()}
return a \code{dock_layout}; \code{is_dock_layout()} a boolean.
}
\description{
A \code{dock_layout} is dockView's own layout representation -- the
\verb{\{grid, panels, activeGroup\}} payload it renders -- the wire form at the
client boundary. It is distinct from our \link[=dock-grid]{dock_grid} (a view's
canonical geometry) and \link[=view]{dock_view()} (a view's structure): a
\code{dock_layout} is what the browser echoes and what a restore pushes back,
geometry fused with resolved panel content in dockView's own shape. It is
produced and consumed only at the dockView seam and is never stored on the
board nor passed through the update lifecycle.
}
\details{
\code{new_dock_layout()} wraps a raw dockView state / payload list (a client
echo) as a \code{dock_layout}; \code{is_dock_layout()} is the class check;
\code{validate_dock_layout()} returns its input and errors on a malformed shape.
Cast across the seam with \code{as_dock_layout()} (build the dockView payload
from a \link[=dock-grid]{dock_grid} against the board's blocks and extensions),
\code{\link[=as_dock_grid]{as_dock_grid()}} (canonical geometry from a layout) and
\link[=view]{as_dock_view()} (membership from a layout).
}
