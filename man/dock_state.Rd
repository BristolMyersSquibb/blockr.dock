% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/external.R
\name{dock_state}
\alias{dock_state}
\title{Read the current dock state}
\usage{
dock_state(session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{session}{The Shiny session. Defaults to the current one.}
}
\value{
A list with \code{views} (character vector of view names),
\code{active_view} (character), and \code{layouts} (a \code{dock_layouts} object),
or \code{NULL} if the dock isn't initialised on this session.
}
\description{
Returns the current view membership, active view, and live layouts
(reflecting any drag-and-drop the user has done). Useful for the MCP
inspection tools that need to surface the live dock state to the LLM.
}
