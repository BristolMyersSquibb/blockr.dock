% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workspace-class.R
\name{dock_workspace}
\alias{dock_workspace}
\alias{is_dock_workspace}
\alias{workspace_ids}
\alias{workspace_layout}
\alias{workspace_layout<-}
\alias{dock_workspaces}
\alias{is_dock_workspaces}
\alias{validate_dock_workspaces}
\alias{active_workspace}
\alias{active_workspace<-}
\alias{ws_can_crud}
\alias{dock_workspaces<-}
\title{Dock workspaces}
\usage{
dock_workspace(layout = list())

is_dock_workspace(x)

workspace_ids(x)

workspace_layout(x)

workspace_layout(x) <- value

dock_workspaces(...)

is_dock_workspaces(x)

validate_dock_workspaces(x)

active_workspace(x)

active_workspace(x) <- value

ws_can_crud(x)

dock_workspaces(x) <- value
}
\arguments{
\item{layout}{Layout specification as a (possibly nested) list of block
and extension IDs, e.g. \code{list("dataset_1", "head_1")} or
\code{list("edit_board_extension", list("a", "b"))}.}

\item{x}{Object}

\item{value}{Replacement value}

\item{...}{Named \code{dock_workspace} objects}

\item{workspaces}{Replacement value}
}
\value{
\code{dock_workspace()} returns a \code{dock_workspace} object.
\code{dock_workspaces()} returns a \code{dock_workspaces} object.
\code{is_dock_workspace()} and \code{is_dock_workspaces()} return booleans.
\code{active_workspace()} returns a string and \verb{active_workspace<-()} returns
the modified \code{dock_workspaces} object invisibly.
\code{workspace_ids()} returns all IDs (block + extension) found in the
layout. The \code{ws_can_crud()} helper returns \code{FALSE} when the dock is
locked.
}
\description{
A \code{dock_board} can contain multiple workspaces (global tabs), each with its
own DockView layout. Blocks and extensions are shared across workspaces
via the board's DAG; workspace membership is a layout concern only.
}
\details{
A single workspace is created with \code{dock_workspace()}, which accepts a
layout specification as a (possibly nested) list of block and extension
IDs — the same format accepted by \code{create_dock_layout(grid = ...)}.
Block and extension membership is derived from that layout.
Multiple workspaces are combined via \code{dock_workspaces()}. Workspace CRUD
is enabled unless the dock is locked (see \code{\link[=is_dock_locked]{is_dock_locked()}}).
}
