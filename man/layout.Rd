% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dock-grid.R, R/dock-rail.R
\name{dock_grid}
\alias{dock_grid}
\alias{panels}
\alias{group}
\alias{default_layout}
\alias{rail}
\title{Dock grid authoring}
\usage{
dock_grid(..., orientation = c("horizontal", "vertical"), sizes = NULL)

panels(..., active = NULL)

group(..., sizes = NULL)

default_layout(blocks, extensions)

rail(
  ...,
  position = c("left", "right", "top", "bottom"),
  active = NULL,
  collapsed = FALSE,
  size = 260,
  collapsed_size = 35
)
}
\arguments{
\item{...}{For \code{dock_grid()} and \code{group()}, grid children (\code{blk()} /
\code{ext()} references, bare ids, character vectors, lists, \code{panels()}, or
\code{group()}). For \code{panels()}, panel references or ids. A reference used
here cannot carry a placement hint. Otherwise reserved for generic
consistency.}

\item{orientation}{Top-level split direction; one of \code{"horizontal"}
(default) or \code{"vertical"}.}

\item{sizes}{Numeric vector parallel to \code{...}, giving each child's
share of the parent (positive; need not sum to 1).}

\item{active}{For \code{panels()} and \code{rail()}, the id of the tab to open by
default.}

\item{blocks, extensions}{Dock board components to arrange (for
\code{default_layout()}).}

\item{position}{For \code{rail()}, the edge the rail pins to; one of \code{"left"}
(default), \code{"right"}, \code{"top"} or \code{"bottom"}.}

\item{collapsed}{For \code{rail()}, whether it opens collapsed to its bare tab
strip. A user collapses a rail by clicking its open tab, and that choice
is stored, so a restored board comes back the way it was left.}

\item{size, collapsed_size}{For \code{rail()}, its width (or height, on a
horizontal edge) in pixels when open and when collapsed to its bare tab
strip.}
}
\value{
\code{dock_grid()} returns a \link[=dock-grid]{dock_grid} object. \code{panels()}
returns a \code{dock_panels} node and \code{group()} returns a \code{dock_group} node --
both are grid sub-trees usable inside \code{dock_grid()} / \code{group()}. A \code{rail()}
is a \code{dock_rail}, which goes to \code{new_dock_board(rails = )} rather than into
a grid. Finally \code{default_layout()} returns a \code{list} with \code{views} (a
\code{dock_views}), \code{grids} (a \code{dock_grids}) and \code{rails} (a \code{dock_rails}).
}
\description{
A \link[=dock-grid]{dock_grid} is a view's geometry: the arrangement of its
panels into nested splits and tab groups, with sizes. \code{dock_grid()} is the
authoring DSL that builds one; view \emph{membership} (which panels belong) and
the display \emph{name} live on the view (see \link[=dock_view]{dock_view()}), not the
grid.
}
\details{
Construct a grid with:
\itemize{
\item \code{dock_grid(...)}: the page-level container. Its \code{...} are the
children of the root branch. Bare strings become single-panel
leaves, character vectors become tabbed leaves, lists become
nested branches. Use \code{\link[=panels]{panels()}} for a tabbed leaf with an explicit
open tab, and \code{\link[=group]{group()}} for a branch with explicit sizes.
\item \code{panels(..., active = NULL)}: a tabbed leaf whose tab strip holds
the given panel ids. \code{active} selects the initially-open tab; the
first id wins by default. A single-panel \code{panels()} is permitted
but redundant (a bare string is equivalent).
\item \code{group(..., sizes = NULL)}: a branch container. \code{sizes} is a
numeric vector parallel to \code{...} that overrides the even split.
\item \code{rail(..., position = "left")}: a rail, that is a tab group pinned
to one edge of the view rather than placed in the grid. Rails are
not grid children: they go to \code{new_dock_board(rails = )}, keyed by
view id, and the members a rail names leave the grid tree.
\item \code{default_layout(blocks, extensions)} produces the default board
arrangement (an extension rail on the left, blocks tabbed in the
grid) as a \code{list(views, grids, rails)} the constructor consumes.
}

\code{dock_grid()} accepts \code{orientation = "horizontal" | "vertical"} for the
top-level split direction and \code{sizes} for the root-branch ratios. The
dockView-native \verb{\{grid, panels, activeGroup\}} payload dockViewR consumes is a
\link[=dock-layout]{dock_layout}, built from a grid against the board's blocks and
extensions.
}
\examples{
blks <- c(
  a = blockr.core::new_dataset_block(),
  b = blockr.core::new_head_block()
)

# Panels named with typed references; bare id strings work too
panels(blk("a"), blk("b"), active = blk("b"))

# Branch with explicit child ratios
group(blk("a"), blk("b"), sizes = c(0.3, 0.7))

# An extension panel beside a tabbed block leaf
dock_grid(
  ext("dag"),
  panels(blk("a"), blk("b"), active = blk("b")),
  sizes = c(0.3, 0.7)
)

# Vertical top-level split
dock_grid(blk("a"), blk("b"), orientation = "vertical")

# An extension parked on the left edge, out of the grid
rail(ext("dag"), position = "left")

}
