% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dock-grid.R, R/dock-layout.R, R/dock-view.R
\name{is_dock_grids}
\alias{is_dock_grids}
\alias{validate_dock_grids}
\alias{board_grids}
\alias{board_grids<-}
\alias{as_dock_view}
\alias{dock_view}
\alias{is_dock_view}
\alias{validate_dock_view}
\alias{view_members}
\alias{is_dock_views}
\alias{validate_dock_views}
\alias{view_name}
\alias{view_name<-}
\alias{view_names}
\alias{active_view}
\alias{active_view<-}
\alias{board_views}
\alias{board_views<-}
\title{Dock views: structure and grid}
\usage{
is_dock_grids(x)

validate_dock_grids(x, views = NULL)

board_grids(x)

board_grids(x) <- value

as_dock_view(x, ...)

dock_view(members = character(), name = NULL)

is_dock_view(x)

validate_dock_view(x)

view_members(x)

is_dock_views(x)

validate_dock_views(x)

view_name(x)

view_name(x) <- value

view_names(x)

active_view(x)

active_view(x) <- value

board_views(x)

board_views(x) <- value
}
\arguments{
\item{x}{An object appropriate to the function: a \code{dock_view} (for
\code{view_name()}, \code{view_members()}), a \code{dock_views} collection (for
\code{view_names()}, \code{active_view()}), a \code{dock_grids}, or a \code{dock_board}
(for the board accessors).}

\item{views}{A \code{dock_views} collection, used to check that grids key
known views.}

\item{value}{Replacement value}

\item{...}{Forwarded to methods.}

\item{members}{Ordered character vector of panel ids.}

\item{name}{Optional display name for the view.}
}
\value{
\code{board_views()} returns a \code{dock_views}, \code{board_grids()} a
\code{dock_grids} or \code{NULL}, and their setters the modified board
invisibly. \code{dock_view()} returns a \code{dock_view} and \code{view_members()} a
character vector. \code{is_dock_view()} / \code{is_dock_views()} /
\code{is_dock_grids()} return a boolean; \code{validate_dock_view()},
\code{validate_dock_views()} and \code{validate_dock_grids()} return their
(validated) input and throw on error. \code{active_view()} returns the active
view's id, or \code{NULL} when no view is active, and \verb{active_view<-()} the
modified collection (or \code{dock_board}) invisibly. \code{view_name()} returns a
view's explicit display name (or \code{NULL}), \verb{view_name<-()} the modified
view, and \code{view_names()} a character vector of display labels keyed by
view id (derived from the id where a view has no explicit name).
\code{as_dock_view()} returns a \code{dock_view}: identity on a \code{dock_view}, or a
view whose members are a \link[=dock-layout]{dock_layout}'s panel ids.
}
\description{
A \code{dock_board} stores its views as two independent slots. \strong{Structure}
-- which panels belong to each view, plus the view names, ids and the
active view -- is a \code{dock_views} collection of \code{dock_view} objects, read
with \code{board_views()}. \strong{Grid} -- the geometry of each view (nesting, tab
groups, sizes) -- is a separate, \code{NULL}-valid \code{dock_grids} slot, read with
\code{board_grids()}. Single-page boards are a degenerate case: one auto-named
"Page" view. Blocks and extensions are shared across views via the board's
DAG; view membership is a layout concern only.
}
\details{
Each view carries a stable, immutable \strong{id} (its key in the collection)
distinct from its editable display \strong{name}. This mirrors the id / name
separation used for blocks (an immutable id keys the collection;
\code{\link[blockr.core:block_name]{blockr.core::block_name()}} is an editable label). The id is minted once
when the view is created and never changes -- rename only rewrites the
name attribute, never the key. Use \code{dock_view()} to construct a view,
\code{view_name()} / \verb{view_name<-()} to read and write its display name,
\code{view_names()} for all names in a collection, and \code{view_members()} for a
\code{dock_view}'s ordered panel-id set.

Multi-view boards are defined by passing \code{views} (and optionally \code{grids})
to \code{new_dock_board()}: \code{views} is a named list keyed by view id (minted
when absent), each value a \code{\link[=dock_view]{dock_view()}}, a bare character vector of member
panel ids, or a list of panel ids. \code{grids} is a named list keyed by view id
whose values are \code{\link[=dock_grid]{dock_grid()}}s; it is optional -- a view with no grid entry
falls back to a default grid over its members wherever placement geometry
is needed. Bare block / extension ids in either slot are resolved to
canonical panel ids against the board's blocks and extensions. The
initially-active view is chosen by \code{new_dock_board(active = )} (a view id),
defaulting to the first; it is a property of the collection, never of an
individual view. View CRUD is enabled unless the dock is locked (see
\code{is_dock_locked()}).

Structure and grid are related by total semantics, not containment: a
member with no grid entry is an un-landed intent, a grid entry with no
membership an inert ghost. Both are legal on a committed board and
reconciled only where placement is read (\code{view_grid()} prunes ghosts and
shows un-landed members via a default) -- the board is valid with no grid
at all. Referential integrity still holds: every member must reference a
block or extension on the board.
}
\examples{
brd <- new_dock_board(
  blocks = c(
    dataset_1 = blockr.core::new_dataset_block(),
    head_1 = blockr.core::new_head_block()
  ),
  views = list(
    analysis = dock_view(c("dataset_1", "head_1"), name = "Analysis"),
    overview = "dataset_1"
  ),
  active = "overview"
)
view_names(board_views(brd))
view_members(board_views(brd)[["analysis"]])

}
