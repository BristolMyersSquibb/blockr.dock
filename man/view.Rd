% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layouts-class.R
\name{new_dock_layouts}
\alias{new_dock_layouts}
\alias{validate_dock_layouts}
\alias{is_dock_layouts}
\alias{view_name}
\alias{view_name<-}
\alias{view_names}
\alias{active_view}
\alias{active_view<-}
\alias{board_layouts<-}
\alias{board_layouts}
\alias{as_dock_layouts}
\title{Dock views (layouts)}
\usage{
new_dock_layouts(...)

validate_dock_layouts(x)

is_dock_layouts(x)

view_name(x)

view_name(x) <- value

view_names(x)

active_view(x)

active_view(x) <- value

board_layouts(x) <- value

board_layouts(x)

as_dock_layouts(x, ...)
}
\arguments{
\item{...}{Generic consistency}

\item{x}{A \code{dock_layout} (for \code{view_name()} / \verb{view_name<-()}) or a
\code{dock_layouts} collection (for \code{view_names()}).}

\item{value}{Replacement value}
}
\value{
\code{is_dock_layouts()} returns a boolean.
\code{validate_dock_layouts()} returns its input and throws on error.
\code{active_view()} returns the active view's id, or \code{NULL} when no
view is active, and \verb{active_view<-()} returns the modified
\code{dock_layouts} (or \code{dock_board}) object invisibly. \code{view_name()}
returns a single display name (or \code{NULL}), \verb{view_name<-()} the
modified \code{dock_layout}, and \code{view_names()} a character vector of
display names keyed by view id.
}
\description{
A \code{dock_board} always holds a \code{dock_layouts} object (multi-view tabs).
Single-page boards are a degenerate case: one auto-named "Page" view.
Blocks and extensions are shared across views via the board's DAG;
view membership is a layout concern only.
}
\details{
Each view carries a stable, immutable \strong{id} (the key into the
\code{dock_layouts} collection) that is distinct from its editable display
\strong{name}. This mirrors the id / name separation used for blocks (an
immutable id keys the collection; \code{\link[blockr.core:block_name]{blockr.core::block_name()}} is an
editable label). The id is minted once when the view is created and
never changes — rename only rewrites the name attribute, never the
key. Use \code{view_name()} / \verb{view_name<-()} to read and write a view's
display name and \code{view_names()} for all names in a collection.

Multi-view boards are defined by passing a named list to
\code{new_dock_board()}'s \code{layouts} argument: each \strong{name} is a view's
display label, each value is the panel arrangement inside that view (a
\code{\link[=dock_layout]{dock_layout()}}, or a raw list of block / extension IDs). A view can be
marked as the initially-active one by passing \code{active = TRUE} to
\code{\link[=dock_layout]{dock_layout()}}; if none is marked, the first one is used. View CRUD is
enabled unless the dock is locked (see \code{is_dock_locked()}).

Users do not normally construct a \code{dock_layouts} directly; instead
they pass a plain named list to \code{new_dock_board(layouts = ...)},
which validates, mints view ids and wraps it.
}
\examples{
brd <- new_dock_board(
  blocks = c(
    dataset_1 = blockr.core::new_dataset_block(),
    head_1 = blockr.core::new_head_block()
  ),
  layouts = list(
    Analysis = list("dataset_1", "head_1"),
    Overview = dock_layout("dataset_1", active = TRUE)
  )
)
view_names(board_layouts(brd))

}
\keyword{internal}
