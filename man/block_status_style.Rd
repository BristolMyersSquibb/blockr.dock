% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plugin-block.R
\name{block_status_style}
\alias{block_status_style}
\title{Block eval-status indicator styling}
\usage{
block_status_style(status)
}
\arguments{
\item{status}{A block eval status. Only \code{waiting}, \code{unset} and \code{failed}
carry an indicator; any other value -- including \code{ready}, \code{dormant} or a
non-string -- yields \code{NULL}.}
}
\value{
\code{NULL} when \code{status} carries no indicator, otherwise a list with
components \code{color} (hex), \code{size} (pixel diameter), \code{placement} and
\code{label}.
}
\description{
Visual specification for the coloured dot that marks a block's eval status
(the \code{waiting} / \code{unset} / \code{failed} states described in
\code{\link[blockr.core:block_server]{blockr.core::block_server()}}). Front-ends share this so the dot renders
with a consistent colour, size and placement: the dock draws it on the block
card icon, and blockr.dag draws it as a node badge.
}
\examples{
block_status_style("waiting")
block_status_style("ready")

}
